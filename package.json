{
  "name": "payai-sh",
  "version": "0.0.0",
  "private": true,
  "description": "The payment policy layer for AI agents, built on x402.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "tsc -b packages/core packages/x402 && node --test packages/*/test/*.test.mjs",
    "demo": "node examples/research-agent/demo.mjs",
    "dev": "python3 -m http.server 5173",
    "publish:dry-run": "npm publish --workspace @payai-sh/core --dry-run --access public && npm publish --workspace @payai-sh/x402 --dry-run --access public"
  },
  "keywords": [
    "x402",
    "ai-agents",
    "payments",
    "policy",
    "receipts"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jerrywang33/payai-sh.git"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
